[package]
name = "stop_family"
version = "0.0.1"
edition = "2018"

[dependencies]
winapi = {version = "0.3.9", features = ["handleapi", "processthreadsapi", "winnt", "securitybaseapi", "impl-default"]}
